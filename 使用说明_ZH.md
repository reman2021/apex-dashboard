# Apex Dashboard 详细中文使用说明

版本日期：2026.06.10

## 1. 插件简介

Apex Dashboard 是一个 Obsidian 工作台插件。它把备忘、待办、项目、数据库、快捷操作、最近文档、番茄钟、热力图、倒数日和正在阅读集中到一个页面里，适合用作每日入口、项目总览、学习记录或个人知识库的控制台。

它的核心思路是：界面负责快速操作，数据仍保存在 Vault 中的 Markdown 文件里。你可以在工作台界面中添加卡片、勾选任务、配置横幅，也可以打开对应的 dashboard Markdown 文件手动调整结构。

## 2. 安装与启用

### 2.1 手动安装

1. 下载插件发布包中的 `main.js`、`styles.css` 和 `manifest.json`。
2. 在当前 Vault 中进入 `.obsidian/plugins/`。
3. 新建文件夹，例如 `apex-dashboard`。
4. 将三个文件放入该文件夹。
5. 回到 Obsidian 设置，在“第三方插件”中启用 Apex Dashboard。

### 2.2 首次打开工作台

启用插件后，可以通过两种方式打开：

- 点击左侧功能区的主页图标。
- 打开命令面板，执行 `Apex Dashboard: Open dashboard`。

首次打开时，插件会根据设置中的“仪表盘文件”创建或读取 dashboard Markdown 文件。默认路径为 `dashboard.md`。

## 3. 工作台基础操作

### 3.1 修改工作台名称

1. 打开 Obsidian 设置。
2. 进入 `Apex Dashboard` 设置页。
3. 找到“工作台名称”。
4. 输入你想显示的名称，例如“每日工作台”或“学习控制台”。
5. 清空该项时，会回退显示默认名称“工作台”。
6. 重启 Obsidian 后生效。

该名称用于工作台标签页显示，刷新或重启 Obsidian 后仍会保留。

### 3.2 启动时自动打开工作台

1. 打开 Obsidian 设置。
2. 进入 `Apex Dashboard` 设置页。
3. 找到“启动时打开工作台”。
4. 开启后，打开库时插件会自动将工作台作为主工作区第一个标签页打开。

该功能默认关闭，适合希望把工作台固定为 Vault 入口页的用户。

### 3.3 dashboard 文件保存在哪里

默认数据文件是 Vault 根目录下的 `dashboard.md`。你可以在插件设置里的“仪表盘文件”修改路径，例如：

```text
dashboard
```

或：

```text
Meta/dashboard
```

设置时不需要填写 `.md` 后缀。

### 3.4 数据如何同步到 Markdown

工作台界面中的主要数据会写入 dashboard Markdown 文件：

- 横幅名言、作者、背景图片、名言颜色、名言库路径。
- 快捷操作。
- 分区名称、颜色和类型。
- 卡片标题、内容、任务、归档状态、项目文档链接、封面图片等。
- 数据库分区配置，例如筛选属性、选中值、视图和分组。
- 倒数日、正在阅读和小组件相关设置。

如果你直接编辑 dashboard Markdown 文件，插件会重新解析并刷新工作台。建议一次只改一小段，避免 YAML frontmatter 格式错误。

### 3.5 管理分区和卡片

- 点击“添加分区”可以创建新分区。
- 双击分区标题可重命名分区。
- 点击卡片上的编辑按钮可修改卡片内容。
- Todo 卡片内可以添加、勾选、拖拽排序任务。
- Projects 和 Notes 卡片可添加文件链接或文档列表。

删除、重命名或大幅调整分区结构时，也可以直接编辑 `dashboard.md` 中对应的 `## 分区名` 内容块。

## 4. 横幅 Banner

### 4.1 打开横幅编辑

横幅右下角有一个线条编辑图标。鼠标悬浮时会出现提示，点击即可打开编辑弹窗。你也可以双击横幅打开同一个弹窗。

### 4.2 修改名言和作者

1. 打开横幅编辑弹窗。
2. 在“名言”中填写显示文本。
3. 在“作者”中填写署名。
4. 点击“保存”。

### 4.3 设置背景图片

横幅编辑弹窗中的“背景图片”支持填写一张或多张图片。每一项都可以是 Vault 相对路径，也可以是网络图片地址。

Vault 图片示例：

```text
Attachments/banner.jpg
```

网络图片示例：

```text
https://example.com/banner.jpg
```

如果图片不显示，优先检查路径大小写、文件是否仍在 Vault 中、网络图片是否能直接访问。

添加多张背景图片时，横幅会轮播显示。当前轮播间隔约为 30 分钟；如果只添加一张，则固定显示这一张。

### 4.4 设置名言颜色

1. 打开横幅编辑弹窗。
2. 找到“名言颜色”。
3. 点击圆形色块，选择颜色。
4. 点击“保存”后，横幅名言会立即使用新颜色。
5. 点击色块右侧的重置图标，可恢复默认名言颜色。

重置后保存时，插件会移除自定义 `quoteColor` 配置。

### 4.5 配置名言库路径

名言库路径用于从 Vault 文件中随机读取名言。每次打开工作台时，插件会从有效行中随机选一条显示。

操作步骤：

1. 在 Vault 中新建一个文本或 Markdown 文件，例如 `Meta/quotes.md`。
2. 每行写一条名言，空行会被忽略。作者可写可不写。
3. 打开横幅编辑弹窗。
4. 在“名言库路径”中填写：

```text
Meta/quotes.md
```

5. 保存后重新打开工作台。

名言库示例：

```markdown
少即是多。 | 密斯·凡德罗
把重要的事做慢一点。 |
先完成，再完美。
```

支持的单行格式包括：

```text
名言 | 作者
名言 - 作者
名言
```

作者可以留空。只有名言库中的该行带有作者时，横幅才显示对应作者；未填写作者时，作者区域为空。如果名言库为空、路径不存在、读取失败或没有有效行，插件会回退显示弹窗中手动填写的名言和作者。

### 4.6 名言库和背景图片列表

横幅编辑弹窗不再提供“名言合集”编辑入口。名言随机展示统一通过“名言库路径”实现。背景图片保留列表式编辑：一张图片固定显示，多张图片轮播显示。

## 5. 快捷操作

快捷操作位于工作台侧栏或移动端快捷区域，可用于快速打开文件或执行 Obsidian 命令。

### 5.1 添加文件快捷操作

1. 打开工作台。
2. 找到“快捷操作”区域。
3. 点击添加按钮。
4. 选择“文件”标签。
5. 输入文件名进行搜索。
6. 选择目标文件并保存。

常见用途：

- 快速打开每日笔记。
- 快速打开项目主页。
- 快速打开收件箱或阅读清单。

### 5.2 添加命令快捷操作

1. 点击“快捷操作”的添加按钮。
2. 选择“命令”标签。
3. 搜索 Obsidian 命令名称。
4. 选择命令并保存。

常见命令包括：

- 新建日记。
- 新建笔记。
- 打开命令面板。
- 执行其他已安装插件提供的命令。

### 5.3 新建日记和新建笔记预设

插件内置“新建日记”和“新建笔记”类预设快捷操作。它们依赖 Obsidian 自身或相关插件命令。如果点击没有反应，请检查：

- Daily Notes 核心插件是否启用。
- 命令 ID 是否仍存在。
- 当前 Vault 是否允许创建文件。
- 是否有其他插件拦截或修改了命令。

### 5.4 常见失败原因

- 文件快捷操作目标文件已被移动或重命名。
- 命令快捷操作对应的插件未启用。
- 命令名称变了，但 dashboard 文件中仍保存旧目标。
- 移动端插件加载较慢，首次点击时命令列表尚未完全可用。

遇到问题时，可以打开 `dashboard.md` 检查 `quickActions` 配置。

## 6. 番茄钟

### 6.1 启用番茄钟

1. 打开 Obsidian 设置。
2. 进入 `Apex Dashboard`。
3. 找到“番茄钟”开关。
4. 开启后，工作台侧栏会显示番茄钟小组件。

### 6.2 设置时长

在插件设置中可以配置：

- 专注时长。
- 短休息时长。
- 长休息时长。
- 每 N 次专注后进入长休息。
- 是否自动开始休息。
- 是否开启声音提醒。

### 6.3 设置活动

番茄钟顶部中间显示“设置活动”。点击后可输入本次专注主题，例如：

```text
论文阅读
```

输入框提示为“你要专注于什么？”。按 Enter 保存，按 Esc 取消。近期使用过的活动会以标签形式显示，点击即可复用。

### 6.4 开始、结束和重置

- 点击“开始专注”开始计时。
- 运行中点击“结束”会停止并回到初始专注时长。
- 点击右下角重置图标，会停止当前计时并恢复初始状态。
- 重置不会清空你设置的活动名称。

番茄钟左上角的番茄统计图标会在开始、结束和重置过程中保持显示。

### 6.5 查看统计

点击番茄钟右上角统计图标，可以查看：

- 今日专注时长。
- 累计专注时长。
- 连续天数。
- 活动分布。
- 最近专注记录。

## 7. 热力图和追踪器

### 7.1 启用热力图

1. 打开插件设置。
2. 找到“热力图”。
3. 开启后，在侧栏显示追踪热力图。
4. 填写“跟踪字段”，例如 `mood`、`sleep`、`weight`。
5. 选择时间范围，例如 30 天、90 天、180 天或 365 天。

### 7.2 在日记 frontmatter 中写入字段

热力图和追踪器读取笔记 frontmatter 中的字段。示例：

```yaml
---
mood: 4
sleep: 7.5
exercise: 1
---
```

如果你把“跟踪字段”设置为 `mood`，插件会读取 `mood: 4` 并在热力图中显示对应日期的数据。

### 7.3 配置追踪器卡片

除了侧栏热力图，你也可以在工作台分区中添加追踪器卡片：

1. 点击分区中的添加卡片按钮。
2. 选择追踪器类型。
3. 填写 frontmatter 字段 key。
4. 选择时间范围。
5. 选择图表样式：折线图、柱状图或热力图。

### 7.4 无数据显示时如何排查
 
 ### 7.5 热力图分区
 
 v1.1.4 版本之后，热力图功能从侧边栏迁移到了 Dashboard 主区域的热力图分区中。
 
 #### 7.5.1 创建热力图分区
 
 1. 点击工作台顶部的 "+ 添加分区"。
 2. 在分区类型中，选择"热力图"。
 3. 创建一个名为"习惯追踪"的分区。
 4. 分区创建后会显示"添加追踪字段"按钮。
 
 #### 7.5.2 添加追踪字段
 
 1. 点击分区中的 "+" 按钮。
 2. 在弹窗中填写：
    - 标签：显示名称（如"睡眠时间"）
    - 字段名：日记 frontmatter 中的 key（如 `sleep`）
    - 时间范围：7天 / 14天 / 30天 / 90天 / 180天 / 365天
 3. 保存后，热力图网格立即显示。
 
 #### 7.5.3 管理多个热力图
 
 一个热力图分区可以添加多个追踪字段，每个字段独立显示热力图网格。
 每个热力图条目包含：
 - 标签行：标签名称 + 字段 key 标注 + 设置按钮 + 删除按钮
 - GitHub 风格热力图网格
 - 统计行：当前值 + 连续打卡天数 + 完成率
 
 #### 7.5.4 编辑和删除
 
 - 点击热力图条目的设置图标，可修改字段 key、标签或时间范围。
 - 点击删除图标，确认后移除该追踪字段（不会删除日记数据）。
 
 #### 7.5.5 与旧版本的差异
 
 - 热力图不再使用全局设置（设置页中不再有热力图相关选项）。
 - 每个追踪字段独立配置，不再受限于单个 widgetTrackerKey。
 - 侧边栏不再显示热力图小组件。
 - 已有的 Tracker 类型卡片（Dashboard 分区中的 Widget）不受影响。

优先检查以下项目：

- 日记文件是否真的包含对应 frontmatter 字段。
- 字段名是否完全一致，例如 `mood` 和 `Mood` 是不同字段。
- 字段值是否是可解析的数字。
- 时间范围是否覆盖了有数据的日期。
- 日记文件的日期是否能被插件识别。
- 追踪器卡片或热力图设置的 key 是否为空。

## 8. 倒数日和正在阅读

### 8.1 启用倒数日

1. 打开 Obsidian 设置。
2. 进入 `Apex Dashboard`。
3. 开启“倒数日小组件”。
4. 回到工作台侧栏，点击倒数日右上角设置按钮。
5. 点击“添加倒数日”，最多可添加 3 个。

每个倒数日包含标签和目标日期。点击“设置日期”会弹出日历，选择日期后保存。倒数日固定按剩余天数显示，不再提供小时或分钟显示模式。

### 8.2 管理多个倒数日

侧栏最多同时显示 3 个倒数日，采用紧凑布局。已经到期的条目会显示“已到期”。如果不需要某个条目，可以在倒数日设置弹窗中点击删除按钮后保存。

### 8.3 启用正在阅读

1. 打开 Obsidian 设置。
2. 开启“正在阅读”小组件。
3. 在侧栏点击“开始阅读”。
4. 在书籍信息弹窗中手动填写书名、作者、总页数和封面图片地址。
5. 保存后即可开始计时。

正在阅读不再搜索线上书籍信息，书籍资料全部由你手动填写。封面图片地址可以是网络图片，也可以是 Vault 图片路径。

### 8.4 记录阅读进度

结束阅读时可以记录本次阅读日期、时长、起始页和结束页，也可以标记为已读完。统计页面会显示累计阅读时间、今日阅读、书籍数量、连续天数和最近阅读记录。

## 9. 分区、卡片与归档

### 9.1 Memo、Todo、Projects、Notes、Library 的区别

- Memo：适合随手记录想法、摘录、今日备忘。
- Todo：适合任务清单，可勾选、排序、设置提醒。
- Projects：适合项目卡片，可设置封面、进度和相关文件。
- Notes：适合文档集合或笔记入口。
- Library：适合把 Vault 笔记按属性筛选、分组和浏览，类似轻量数据库。

### 9.2 添加和编辑卡片

1. 在目标分区点击添加卡片。
2. 输入标题或选择卡片类型。
3. 点击卡片编辑按钮修改内容。
4. 对 Todo 卡片，可直接添加任务。
5. 对 Projects 卡片，可添加相关文件链接。

### 9.3 拖拽排序

Todo 卡片内的任务支持拖拽排序。Projects 和 Notes 卡片中的文档链接也支持在卡片之间移动。卡片本身的 hover 效果只会增强阴影和边框，不会向上移动导致遮挡。

### 9.4 Todo 卡片归档

Todo 卡片右上角提供归档操作。归档后，卡片不会继续占用正常 Todo 区域，但仍保存在 dashboard Markdown 文件中。

- 点击 Todo 分区标题右侧的归档按钮，可以查看或隐藏归档卡片。
- 归档列表会显示卡片标题、归档日期和任务完成数量。
- 可以从归档列表中恢复卡片，也可以删除归档卡片。
- 归档列表采用固定高度，归档较多时可在列表内部滚动。

Todo 卡片本身使用固定高度，任务较多时在卡片内部滚动，避免展开归档列表时压缩待办卡片导致漏看任务。

### 9.5 设置项目封面

1. 打开项目卡片编辑。
2. 找到封面图片路径。
3. 填写 Vault 相对路径或网络图片地址。
4. 保存后卡片会显示封面。

### 9.6 添加文件到项目卡片

项目卡片支持添加文件链接。你可以通过搜索添加，也可以把文件路径写入卡片内容。常见格式为：

```markdown
[[Projects/Example]]
```

## 10. 数据库分区

### 10.1 创建数据库分区

1. 点击“添加分区”。
2. 选择 Library 类型。
3. 创建后点击数据库分区右上角配置按钮。

数据库分区会读取 Vault 中 Markdown 文件开头显式写入的 frontmatter 属性。

### 10.2 添加属性筛选

1. 打开数据库配置弹窗。
2. 点击“添加筛选”。
3. 在属性下拉框中选择一个属性。
4. 选择属性后，界面会列出当前库内该属性已有的值。
5. 点击值标签进行选中或取消。
6. 保存后，数据库只显示符合所选属性值的文件。

如果某个属性没有出现在下拉框中，请确认它是否写在笔记开头的 YAML frontmatter 中。

### 10.3 分组和视图

数据库分区支持网格、列表、表格和看板视图。看板视图可以选择一个属性作为分组依据，例如 `status`、`type` 或 `category`。

## 11. 主题与弹窗

工作台主题会影响卡片、侧边栏、小组件和常用编辑弹窗。数据库配置、横幅编辑、卡片编辑、控件配置、倒数日设置等弹窗会继承当前工作台主题，减少视觉割裂。

如果切换主题后某个弹窗仍显示异常，可以关闭弹窗后重新打开；主题变量会在弹窗打开时重新同步。

## 12. 常见问题

### 12.1 为什么热力图没有数据

通常是字段名不一致、frontmatter 格式错误、值不是数字或时间范围不覆盖数据。先打开一篇日记，确认 frontmatter 中存在与设置完全一致的字段。

### 12.2 为什么快捷操作没有反应

检查目标文件是否还存在，或对应命令所属插件是否启用。命令类快捷操作依赖 Obsidian 当前可用命令列表。

### 12.3 为什么图片不显示

检查路径是否是 Vault 相对路径，文件是否被移动，路径大小写是否一致。网络图片还需要确认链接可直接访问。

### 12.4 为什么设置日期按钮没有弹出日历

倒数日的日期选择器会弹出在按钮下方。如果没有显示，优先确认当前插件文件是否已经更新到最新构建后的 `main.js` 和 `styles.css`。旧版本样式可能导致弹层被遮挡或看起来没有反应。

### 12.5 添加多个背景图片会显示哪一张

如果只添加一张背景图片，横幅固定显示这一张。如果添加多张，横幅会按时间轮播，当前间隔约为 30 分钟。增删 Todo 任务不会触发横幅重新轮播。

### 12.6 如何恢复默认配置

可以在插件设置中逐项清空或关闭对应配置。dashboard 内容本身保存在 Markdown 文件中，如果需要恢复默认工作台，可以先备份当前 `dashboard.md`，再创建新的 dashboard 文件路径让插件重新生成默认内容。

### 12.7 dashboard 文件能否手动编辑

可以。建议保留 frontmatter 的 YAML 格式，不要删除 `dashboard: true`、`banner`、`columns` 等关键结构。手动编辑后，工作台会重新解析文件并刷新。
